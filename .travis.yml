language: node_js
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
node_js:
- node
- 7.0.0
services: mongodb
before_script:
- mongo db_test --eval 'db.addUser("test", "test");'
sudo: false
install:
- npm install --no-optional
script:
- DB=mongodb npm test
notifications:
